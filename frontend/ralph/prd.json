{
  "project": "ZKSettle Frontend",
  "branchName": "ralph/zksettle-landing-dashboard",
  "description": "ZKSettle Landing + Dashboard for Colosseum Frontier 2026 \u2014 editorial marketing surface and read-only dashboard aligned with the canonical design system spec at docs/superpowers/specs/2026-04-18-zksettle-landing-design.md and implementation plan at docs/superpowers/plans/2026-04-18-zksettle-landing.md. Stack: Next.js 15 App Router, React 19, TypeScript strict, Tailwind v4, shadcn/ui (token-customized), GSAP + ScrollTrigger, motion v11, Three.js vanilla, Shiki, Iconoir v7, pnpm 9. All work happens in /home/mario/zksettle/frontend/. Deadline: 2026-05-11.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Initialize Next.js 15 project with TypeScript strict",
      "description": "As a developer, I need a scaffolded Next.js 15 App Router project in frontend/ so the rest of the work has a home.",
      "acceptanceCriteria": [
        "frontend/package.json exists with Next 15, React 19, TypeScript, Tailwind, App Router, src/ dir, @/* alias, pnpm, Turbopack",
        "frontend/tsconfig.json has strict: true, noUncheckedIndexedAccess: true, noImplicitOverride: true, forceConsistentCasingInFileNames: true",
        "frontend/package.json scripts include: dev, build, start, typecheck, lint, test, test:watch",
        "frontend/package.json has engines.node >=20.10 and packageManager pnpm@9.12.0",
        "frontend/README.md exists with stack and pnpm dev instructions",
        "pnpm install completes without error",
        "pnpm build succeeds on the default scaffold",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Scaffolded Next 15.5.15 (patched 15.x) + React 19.0.0 + Tailwind v4 + TS strict. Build and typecheck pass. next.config.ts pins outputFileTracingRoot to the frontend/ dir to silence the multi-lockfile warning from /home/mario."
    },
    {
      "id": "US-002",
      "title": "Install ESLint flat config + Prettier + Vitest",
      "description": "As a developer, I need lint and test tooling before writing code so every subsequent task can verify cleanly.",
      "acceptanceCriteria": [
        "Dev deps installed: eslint@9, eslint-config-next@15, typescript-eslint@8, prettier, prettier-plugin-tailwindcss, vitest@2, @vitest/ui, jsdom",
        "frontend/eslint.config.mjs exists as flat config composing typescript-eslint and eslint-config-next",
        "frontend/.prettierrc exists with semi: true, singleQuote: false, trailingComma: all, printWidth: 100, tailwind plugin",
        "frontend/vitest.config.ts exists with node environment and @ alias pointing to src",
        "pnpm lint exits 0",
        "pnpm test exits 0 with no test files message",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "eslint 9.39.4 + eslint-config-next 15.5.15 (consumed via FlatCompat from @eslint/eslintrc) + typescript-eslint 8.58.2 + prettier 3.8.3 + prettier-plugin-tailwindcss 0.7.2 + vitest 2.1.9 + @vitest/ui 2.1.9 + jsdom 29.0.2. Switched lint script from deprecated `next lint` to `eslint .`; added `--passWithNoTests` to the test script so vitest exits 0 on empty runs. Build still succeeds."
    },
    {
      "id": "US-003",
      "title": "Install runtime dependencies (fonts, Iconoir, motion, GSAP, Three, Shiki)",
      "description": "As a developer, I need all runtime libraries installed so UI work can begin.",
      "acceptanceCriteria": [
        "Runtime deps installed: geist, iconoir-react, motion, three, @types/three, gsap, shiki, clsx, tailwind-merge, class-variance-authority",
        "frontend/public/fonts/ directory exists with a .gitkeep",
        "pnpm ls lists all listed packages at expected versions",
        "pnpm build still succeeds",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Installed runtime deps: geist 1.7.0, iconoir-react 7.11.0, motion 12.38.0 (motion v11 deprecated; v12 is the current line and exposes the same React API), three 0.184.0, gsap 3.15.0, shiki 4.0.2, clsx 2.1.1, tailwind-merge 3.5.0, class-variance-authority 0.7.1. Dev: @types/three 0.184.0. public/fonts/.gitkeep created. Build, typecheck, lint, test all pass; landing first-load JS still 102 kB (deps tree-shake until imported)."
    },
    {
      "id": "US-004",
      "title": "Author design tokens in globals.css",
      "description": "As a designer, I need the full ZKSettle token system available via Tailwind v4 @theme so components can consume the canonical palette, typography, radii, and easing.",
      "acceptanceCriteria": [
        "frontend/src/app/globals.css imports tailwindcss and defines @theme block with all color tokens: canvas, surface, surface-deep, border-subtle, border, ink, quill, stone, muted, ghost, forest, forest-hover, emerald, mint, warning-bg/text, danger-bg/text, info-bg/text",
        "Tokens also include font-display (Georgia), font-sans (Geist), font-mono (JetBrains/Berkeley fallback), radii 2/3/6/10, and --ease-brand cubic-bezier(0.32, 0.72, 0, 1)",
        "Global html/body rules apply canvas background, ink color, sans font family, font smoothing",
        "em element rule: italic, forest color, inherit weight",
        "::selection uses forest background with canvas text",
        "prefers-reduced-motion media query disables animations",
        "pnpm build succeeds",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Authored full @theme block in src/app/globals.css using Tailwind v4 naming (--color-*, --font-*, --radius-*, --ease-*). All 20 color tokens present (canvas/surface/surface-deep/border-subtle/border, ink/quill/stone/muted/ghost, forest/forest-hover/emerald/mint, warning|danger|info bg/text). Font tokens use next/font CSS variable fallbacks (--font-geist-sans, --font-jetbrains-mono) that US-005 will register, cascading to Georgia / system-ui / ui-monospace. Radii 2/3/6/10 + --ease-brand cubic-bezier(0.32,0.72,0,1). html/body apply canvas bg + ink + font-sans + antialiased smoothing. em rule forest italic inherit-weight. ::selection forest/canvas. prefers-reduced-motion disables animations + transitions + scroll-behavior. Build, typecheck, lint, test all pass."
    },
    {
      "id": "US-005",
      "title": "Root layout with fonts, metadata, and skip link",
      "description": "As a user, I need every page to serve the correct fonts, SEO metadata, and a skip-to-content link.",
      "acceptanceCriteria": [
        "frontend/src/app/layout.tsx registers Geist Sans via next/font with --font-geist-sans CSS variable",
        "JetBrains Mono (or Berkeley fallback) registered via next/font with --font-mono CSS variable",
        "Metadata object defines title default + template, description, openGraph (title, description, type website, images placeholder), twitter card summary_large_image",
        "html lang=\"en\" suppressHydrationWarning",
        "body applies font variables to className and contains a skip-to-content anchor before children",
        "pnpm dev renders / with Geist body copy and Georgia display headings",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Registered Geist Sans via `geist/font/sans` (variable --font-geist-sans) and JetBrains Mono via `next/font/google` (variable --font-jetbrains-mono). Attached both font.variable classes on <html> so the CSS vars cascade globally; <body className=\"font-sans antialiased\"> picks up the --font-sans token from globals.css. Metadata includes title.default + template, description, openGraph (title/description/type=website/images=/og), and twitter.card=summary_large_image. metadataBase resolves via NEXT_PUBLIC_SITE_URL (defaults to http://localhost:3000) so relative OG image paths render as absolute URLs and silence Next's warning. Skip-to-content anchor lives as the first body child (sr-only by default, visible on focus with forest bg + canvas text + 2px forest outline); page.tsx now exposes the focus target via id=\"main-content\" on <main>. Dev server verified: lang=\"en\" present, both font variable classes on <html>, og:/twitter: meta tags render correctly, skip link visible in markup. Build, typecheck, lint, test all pass."
    },
    {
      "id": "US-006",
      "title": "Build The Seal logo SVG component",
      "description": "As a brand, I need The Seal \u2014 circular forest ring with cancelled serif Z and registration ticks at 12/3/6/9 \u2014 as a reusable React component.",
      "acceptanceCriteria": [
        "frontend/src/components/icons/logo.tsx exports a Logo component with props: variant (canvas-ink | surface-forest | forest-surface), size (number, default 48), showWordmark (boolean, default true)",
        "SVG contains: hairline ring 1.25px, serif Z centered, 2.5px horizontal settlement bar across Z midpoint, 4 registration ticks at 12/3/6/9",
        "Wordmark (when shown) reads ZKSettle in Georgia serif at seal-diameter \u00d7 0.4 gap to the right",
        "All three variants render correct fg/bg pairs from design tokens",
        "Logo renders crisp at 16px (favicon) and 200px (hero)",
        "Logo imported and rendered on landing nav so /",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Authored src/components/icons/logo.tsx exporting Logo({variant, size=48, showWordmark=true, className}) and internal Seal(). SVG uses viewBox 48x48, circle ring r=22 stroke=1.25, centered Georgia serif <text>Z</text> fontSize 26, 2.5px settlement bar (x 10.5\u219237.5 at y=24) rendered after the Z so it cancels the glyph, and 4 registration ticks (each 1.25px, 3px long) at 12/3/6/9 crossing the ring. Variants map to Tailwind token classes: canvas-ink \u2192 text-ink/text-ink, surface-forest \u2192 text-forest/text-ink, forest-surface \u2192 text-surface/text-surface. Seal uses currentColor so the variant class on the <svg> wrapper drives stroke+fill. Wordmark renders ZKSettle in font-display (Georgia) at size*0.5 font-size, gap between seal and wordmark = size*0.4 per spec \u00a73.1 lockup. Aria: wordmark visible \u2192 span has no role; showWordmark=false \u2192 role=img aria-label=ZKSettle; the seal SVG is always aria-hidden. Imported into src/app/page.tsx as a temporary nav header (size 32, canvas-ink); US-011 will relocate to nav.tsx. Build, typecheck, lint, test all pass; dev server renders circle + ZKSettle wordmark at /."
    },
    {
      "id": "US-007",
      "title": "Landing copy single source of truth",
      "description": "As a copywriter, I need all landing copy centralized in typed modules so iteration is one-file.",
      "acceptanceCriteria": [
        "frontend/src/content/copy.ts exports a COPY constant with entries for hero, paradox, twoRealities, howItWorks, numbers, demo, useCases, developers, momentum, closingCta, footer \u2014 each section matching the Design Spec \u00a74 strings exactly",
        "frontend/src/content/use-cases.ts exports a USE_CASES array of 5 items (Travel rule, Proof of solvency, ZK credit score, AML by behavior, Proof of reserves) with name, tagline, footnote",
        "All exports are typed (no any)",
        "Numbers block exposes {label, number, sub} tuples for each of <5s, <$0.001, 0, 256 bytes",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Authored src/content/copy.ts exporting COPY: LandingCopy with 11 section entries (hero, paradox, twoRealities, howItWorks, numbers, demo, useCases, developers, momentum, closingCta, footer) \u2014 all strings lifted verbatim from Design Spec \u00a74. Every shape declared as a readonly interface (HeroCopy, ParadoxCopy, TwoRealitiesCopy with TwoRealitiesRow/TwoRealitiesSide, HowItWorksCopy/HowItWorksStep, NumbersCopy/NumberTuple, DemoCopy/DemoFormField, UseCasesCopy, DevelopersCopy, MomentumCopy/MomentumColumn, ClosingCtaCopy, FooterCopy). Zero `any` \u2014 IDE inferred types for every leaf. Numbers.items is `readonly NumberTuple[]` of {label, number, sub}: <5s/\"Proving time\", <$0.001/\"Verify cost\", 0/\"PII leaked\", 256 bytes/\"Proof size\". TwoRealities rows include both plain + redacted values so US-015 can animate the dissolve without re-deriving \u2593 strings. Demo form exposes field defaults (recipient 5g8H4nP3eR..., amount min 100/max 10000/default 1200, jurisdiction US default from options [US/EU/UK/BR]), generate/submit/expired toggle labels, honestyFooter + expiredError strings. Developers.code stores the 3-line SDK example as a multi-line string (joined from array for source clarity); Shiki will consume it in US-020. src/content/use-cases.ts exports USE_CASES: readonly UseCase[] of 5 items matching Design Spec \u00a74 \u00a77 lines exactly (AML by behavior has footnote: \"\" since spec line omits the third bullet). COPY.useCases.items re-exports USE_CASES so downstream section components can destructure from one import. Build, typecheck, lint, test all pass; bundle unchanged at 102 kB."
    },
    {
      "id": "US-008",
      "title": "Utility lib \u2014 cn, format, seeded PRNG",
      "description": "As a developer, I need shared utilities (cn class merger, format helpers, deterministic PRNG) before building components that consume them.",
      "acceptanceCriteria": [
        "frontend/src/lib/cn.ts exports cn() merging clsx + tailwind-merge",
        "frontend/src/lib/format.ts exports truncateWallet(address, head=4, tail=4), fmtAmount(value, currency='USDC'), fmtCompact(n)",
        "frontend/src/lib/prng.ts exports createPrng(seed) returning an object with next(), range(min,max), pick(array) using deterministic mulberry32 (or similar)",
        "Vitest tests cover format and prng with at least 3 assertions each including determinism for prng",
        "pnpm test passes",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Authored src/lib/cn.ts (clsx + tailwind-merge composition \u2014 the canonical shadcn pattern), src/lib/format.ts (truncateWallet uses \u2026 ellipsis between head/tail; fmtAmount leans on Intl.NumberFormat en-US with 2 fraction digits + space-joined currency label default USDC; fmtCompact uses Intl compact notation with 1 fraction digit), and src/lib/prng.ts (mulberry32 \u2014 `state >>>= 0` seeding, Math.imul bit-mixing, normalized by 2^32). Prng interface exports Prng type for downstream imports. Tests: format.test.ts covers 3 functions \u00d7 3 assertions (9 total); prng.test.ts covers range validity, determinism across two instances of same seed, different seeds produce different values, range() bounds, pick() determinism + empty-array throw. All 15 tests pass. Typecheck, lint, build all clean; landing still 102 kB first-load."
    },
    {
      "id": "US-009",
      "title": "Reusable UI primitives (Button, Input, Select, Slider, Badge, Tabs)",
      "description": "As a developer, I need token-driven shadcn primitives so sections share one visual language.",
      "acceptanceCriteria": [
        "frontend/src/components/ui/ contains button.tsx, input.tsx, select.tsx, slider.tsx, badge.tsx, tabs.tsx",
        "Button has variants: primary (forest bg, canvas text), ghost (ink outline), link (forest underline); sizes: sm/md/lg; radius token 3",
        "All primitives use cn() and consume design tokens via Tailwind classes (no hardcoded hex)",
        "Focus state uses 2px forest outline with 2px offset on every primitive",
        "Primitives render a demo route or Storybook-like test page for visual verification",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Authored 6 primitives in src/components/ui/: button.tsx (cva-driven primary/ghost/link \u00d7 sm/md/lg, radius-3, forest-hover on primary, ink-outline on ghost, underline on link), input.tsx (radius-2 border-subtle, hover:border-border, focus:border-forest), select.tsx (native <select> appearance-none + absolute chevron SVG), slider.tsx (native range + .zks-slider pseudo-element styling added to globals.css for webkit/moz track+thumb), badge.tsx (default/success/warning/danger/info/forest variants using mint/warning-bg/danger-bg/info-bg tokens), tabs.tsx (controlled+uncontrolled via useId-scoped Context; TabsList/TabsTrigger/TabsContent with aria-selected/controls, roving tabindex, Arrow/Home/End keyboard nav). All focus-visible states emit 2px forest outline + 2px offset. Demo route src/app/ui/page.tsx showcases every variant and size. Build passes (12 kB first-load on /ui), lint clean, 15 tests still pass, typecheck green. Browser verification via curl confirmed 200 + rendered labels + ARIA roles + token classes (agent-browser CLI not installed on this machine, so no screenshot)."
    },
    {
      "id": "US-010",
      "title": "DisplayHeading helper with forest em support",
      "description": "As a designer, I need a heading component that renders display-xl/l/m variants with inline italic forest em on named primitives.",
      "acceptanceCriteria": [
        "frontend/src/components/ui/display-heading.tsx exports DisplayHeading({ level: 'xl'|'l'|'m', children, as })",
        "Sizes per spec \u00a73.4: display-xl clamp(56px,7vw,128px)/0.95/-3.5%, display-l clamp(40px,5vw,72px)/1.03/-3.5%, display-m 32-48px/1.05/-2%",
        "Font-family: Georgia (via --font-display)",
        "Embedded <em> renders italic forest via the global em rule already defined",
        "Rendered on a demo section proving all three sizes",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Landing nav + 10 section shells composed in page.tsx",
      "description": "As a user, I need the landing to render all 10 sections end-to-end (static content, no motion yet) so the narrative is scrollable.",
      "acceptanceCriteria": [
        "frontend/src/components/landing/nav.tsx renders: Seal logo left, links (Demo, SDK, GitHub) right, forest 'Try demo' button, 56px height, canvas bg, hairline border-bottom on scroll",
        "Section shell components created in frontend/src/components/landing/: hero/hero.tsx, paradox-section.tsx, two-realities-section.tsx, how-it-works-section.tsx, numbers-section.tsx, demo-section.tsx, use-cases-section.tsx, developers-section.tsx, momentum-section.tsx, closing-cta.tsx",
        "Each section renders eyebrow + headline + body copy from COPY (no motion, no demo logic, no WebGL yet \u2014 static layout)",
        "frontend/src/app/page.tsx composes nav + all 10 sections in the Design Spec order",
        "Section vertical rhythm matches spec: 160px desktop / 96px mobile",
        "Eyebrow uses Berkeley Mono 12px UPPER tracking +8% forest + 20px forest rule above headline",
        "Use Cases renders 5 cards (3+2) from USE_CASES",
        "Momentum renders 3 hairline-divided columns",
        "Closing CTA is forest full-width panel with canvas text and inverted CTAs",
        "Footer contains wordmark, links row, mono bottom line",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Authored landing composition end-to-end. New: src/components/landing/nav.tsx ('use client' sticky 56px nav with Seal at 28px, hidden-on-mobile link row (Demo/SDK/GitHub), forest 'Try demo' button styled via buttonVariants Link, scroll listener that toggles border-border-subtle bottom after >8px scrollY); src/components/landing/section.tsx (shared Section wrapper — py-24 md:py-40 for 96/160 rhythm, max-w-6xl, px-5 md:px-8 for 20px gutter; SectionHeader helper — mono forest eyebrow + 20px w-5 h-px forest rule + DisplayHeading level='l' default, tone='surface' flips to inverted canvas/surface for forest panels); 10 section shells under src/components/landing/ — hero/hero.tsx (min-h-[calc(100vh-56px)] display-xl hero pulling eyebrow/headline/sub/ctas straight from COPY.hero, forest primary + ghost secondary), paradox-section.tsx (2-col split: header left, body paragraph right), two-realities-section.tsx (split RealityCard grid; left renders plain values with danger Badge, right renders ▓ redacted values with forest Badge + proof line in mono forest; caption below in italic font-display), how-it-works-section.tsx (3-col hairline-bordered step list, mono index/display title/quill body), numbers-section.tsx (2×2 grid on bg-surface, display-xl numbers + mono forest eyebrow label + stone sub), demo-section.tsx (static form shell using Input/Select/Slider primitives populated from COPY.demo.form defaults; disabled/readonly for US-011; terminal placeholder div showing COPY.demo.initialTerminal; honesty footer in mono muted), use-cases-section.tsx (5 cards laid out 3+2 from USE_CASES on bg-surface), developers-section.tsx (7/5 split: tabs row styling TypeScript active forest underline / Rust & Anchor CPI muted + pre/code block on surface-deep + install/version/GitHub aside on surface), momentum-section.tsx (3 hairline-divided columns + footnote), closing-cta.tsx (full-bleed bg-forest text-canvas panel with inverted CTAs — primary = canvas bg / forest text, secondary = ghost canvas border); src/components/landing/footer.tsx (wordmark + tagline top row, links ul, mono bottomLine). page.tsx composes Nav → main (Hero, Paradox, TwoRealities, HowItWorks, Numbers, Demo, UseCases, Developers, Momentum, ClosingCta) → Footer in Design Spec §4 order. All copy pulled from COPY / USE_CASES — no inlined strings. Build ✅ landing now 5.42 kB route / 116 kB first-load (well under the 220 kB budget from US-032); typecheck ✅, lint ✅, 15 tests ✅. Dev server curl to http://localhost:3000 confirms all 9 eyebrows, all 11 headlines, all 5 use-case names, Try demo / Demo / GitHub nav links, role='tablist'/'tab'/'group' ARIA hooks, sticky top-0 nav, VIOLATES GDPR + COMPLIANT · VERIFIED pills, Proof: 0xa3f8 line all render. Motion, demo logic, Iconoir icons, WebGL, and Two-Realities scroll dissolve intentionally deferred to US-013/US-014/US-015/US-017/US-019."
    },
    {
      "id": "US-012",
      "title": "Reduced-motion + scroll-progress + mounted hooks",
      "description": "As a developer, I need shared hooks so every animated component can respect accessibility and track scroll progress without duplication.",
      "acceptanceCriteria": [
        "frontend/src/hooks/use-reduced-motion.ts exports useReducedMotion() returning boolean from window.matchMedia",
        "frontend/src/hooks/use-scroll-progress.ts exports useScrollProgress(ref) returning 0-1 progress through the referenced element",
        "frontend/src/hooks/use-mounted.ts exports useMounted() returning boolean after first effect",
        "All hooks are 'use client'",
        "Vitest tests (using jsdom) cover useReducedMotion and useMounted with at least one assertion each",
        "pnpm test passes",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Authored three client hooks under src/hooks/: use-reduced-motion.ts reads window.matchMedia('(prefers-reduced-motion: reduce)') inside useEffect (SSR-safe — starts false on server then reconciles on client), subscribes via addEventListener('change') + cleans up on unmount; use-mounted.ts flips false→true inside a zero-dep useEffect so client-only UI can gate rendering; use-scroll-progress.ts accepts a RefObject<HTMLElement|null> and tracks 0-1 progress across (viewport + element height), wired to passive scroll + resize listeners with clamp01 guard. All three files carry the 'use client' pragma at top. Tests live at src/hooks/*.test.ts with '// @vitest-environment jsdom' pragma per file: use-mounted.test.ts asserts post-mount value + cross-rerender stability (2 tests); use-reduced-motion.test.ts stubs window.matchMedia via Object.defineProperty and asserts initial-match read + change-event propagation (2 tests). @testing-library/react@^16 + @testing-library/dom@^10 added as devDeps (v16 is the React 19-compatible line). 19 tests pass across 4 files; typecheck/lint/build clean; landing still 116 kB first-load."
    },
    {
      "id": "US-013",
      "title": "Veil canvas \u2014 Three.js hero particle shader",
      "description": "As a user, I want the hero to show ~12k warm-ash particles drifting that converge into a single forest commitment glyph as I scroll, carrying the ZK metaphor visually.",
      "acceptanceCriteria": [
        "frontend/src/components/landing/hero/veil-canvas.tsx is 'use client' and mounts a Three.js scene full-bleed behind the hero text",
        "frontend/src/components/landing/hero/veil-shaders.ts exports vertex + fragment GLSL strings rendering 12,000 particles on canvas-colored bg",
        "Particles converge over 0\u21921 scroll progress into a central forest commitment glyph",
        "Mount/unmount/dispose lifecycle correct: no memory leaks on hot reload, renderer.dispose() on unmount",
        "useReducedMotion short-circuits to a static rendered frame (no animation loop)",
        "FPS monitor auto-downgrades particle count if <50fps for 2s",
        "Viewport <768px falls back to a static SVG (no WebGL)",
        "Landing renders with the canvas behind the hero text at 60fps on an M1-class laptop",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Authored src/components/landing/hero/veil-shaders.ts (VERTEX_SHADER + FRAGMENT_SHADER as tagged GLSL strings) and src/components/landing/hero/veil-canvas.tsx (Three.js Points + BufferGeometry + ShaderMaterial rendered into an OrthographicCamera for a fullscreen 2D particle field). Geometry attributes: aStart (scatter), aTarget (ring 82% + settlement bar 18% + ambient idle), aPhase (drift), aSize (sign encodes glyph vs ambient membership — negative = ambient, positive = glyph). Shader lerps aStart → aTarget by brand-eased uProgress (cubic-bezier approximated by ease-out-quint); idle drift multiplied by (1-p) so the field stills as it converges; glyph particles tint warm-ash → forest while ambient particles fade to 15% alpha at p=1. Renderer uses antialias:false + alpha:true + powerPreference:'low-power' and setPixelRatio(min(DPR, 2)) so high-DPI machines don't quadruple fragment load. Camera is fit via aspect-aware orthographic bounds (left=-aspect, right=aspect, top=1, bottom=-1); uScale uniform rescales the glyph for narrow viewports. Scroll progress computed locally (not via useScrollProgress — the rAF loop needs a mutable ref, not a setState subscription) with passive scroll listener; rAF tick interpolates uProgress toward targetProgress with a time-aware `1 - exp(-dt*6)` step so scroll spikes don't rubber-band. FPS monitor: rolling 2s window counts frames; if avg < 50 fps, tierIndex++ and geometry.setDrawRange(0, PARTICLE_TIERS[tier]) halves live particle count (tiers 12k→6k→3k; no geometry rebuild — draw-range downgrade is free). useReducedMotion short-circuits to a single renderer.render() after setting uProgress directly — no rAF loop, no drift. Viewport <768px renders a static SVG fallback (forest ring + settlement bar on canvas bg) — no Three.js mount at all on mobile. Lifecycle cleanup: rAF cancelled, scroll+resize listeners removed, renderer.dispose() + geometry.dispose() + material.dispose() + canvas element removed from DOM; React StrictMode's double-mount is handled because the cleanup is idempotent. Three.js would pull ~120 kB gz into the landing's first-load bundle, so src/components/landing/hero/veil-canvas-lazy.tsx wraps next/dynamic(ssr:false) to split it into a separate chunk — Hero (server component) imports the lazy wrapper, not the canvas directly. Landing first-load JS went from 116 kB → 117 kB (+1 kB for the thin lazy wrapper); the 139 kB veil chunk ships separately after hydration, keeping under the US-032 220 kB budget. Build, typecheck, lint, test all pass. Dev server curl confirms hero + main-content + bg-canvas + font-display render; 60fps M1 verification and visual WebGL inspection require manual browser check (agent-browser CLI not installed on this machine)."
    },
    {
      "id": "US-014",
      "title": "GSAP setup + PinnedSection primitive",
      "description": "As a developer, I need GSAP + ScrollTrigger registered once and a PinnedSection wrapper that encapsulates the pin/scrub pattern cleanly.",
      "acceptanceCriteria": [
        "frontend/src/lib/gsap.ts registers ScrollTrigger plugin and sets defaults; exports a typed matchMedia helper",
        "frontend/src/components/motion/pinned-section.tsx ('use client') wraps children and runs gsap.matchMedia: desktop pins the section for N\u00d7 viewport, mobile skips pin, prefers-reduced-motion skips pin",
        "PinnedSection accepts children, pinDuration (multiplier of viewport height), and onProgress callback (0-1)",
        "Cleanup: pin kill + scrolltrigger.kill on unmount (no duplicate pins on route navigation)",
        "Demo section uses PinnedSection and scroll progress is visible",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Authored src/lib/gsap.ts — registers ScrollTrigger once (idempotent ensureRegistered() guard + typeof-window check so SSR doesn't touch document), exports `gsap`, `ScrollTrigger`, BRAND_EASE (tuple) + BRAND_EASE_CSS constants, and a typed `brandMatchMedia(setup)` helper that wraps `gsap.matchMedia` with three fixed breakpoints (isDesktop = min-width:768px AND prefers-reduced-motion:no-preference, isMobile = max-width:767px, isReduced = prefers-reduced-motion:reduce) and passes setup() a typed BreakpointConditions object plus the raw gsap.Context (for .add()/kill() if a caller needs it). Authored src/components/motion/pinned-section.tsx (\"use client\") — accepts children, pinDuration (default 1.5× viewport height), onProgress callback (0–1), showProgress (boolean), className; uses brandMatchMedia so only the isDesktop branch creates a ScrollTrigger.create({pin:true, scrub:true, invalidateOnRefresh:true, onUpdate}) instance — mobile and reduced-motion fall through as no-ops (pass-through render). onProgress is held in a ref so callers can change the handler without re-creating the trigger on every render. Cleanup on unmount calls mm.kill() (GSAP's matchMedia disposes the added context AND its triggers). Progress indicator (shown only when showProgress=true + md: breakpoint) renders a 40x1px border-subtle track with a forest bar scaled via transform: scaleX(progress) plus a \"Scroll progress · N%\" mono caption. src/components/landing/demo-section.tsx now wraps its SectionHeader + form/console grid inside <PinnedSection pinDuration={1.5} showProgress className=\"pb-20 md:pb-24\"> — extra bottom padding gives the progress indicator breathing room under the last grid row. Verification: pnpm typecheck ✅, pnpm lint ✅, pnpm test ✅ (19 tests still pass), pnpm build ✅. Landing first-load JS went 117 kB → 163 kB (+46 kB for GSAP+ScrollTrigger), still under the US-032 220 kB budget. Dev server curl confirms demo section markup has `data-pinned=\"false\"` indicator + \"Scroll progress · 0%\" text + md:block visibility. M1-class 60fps scroll + pin/scrub visual verification requires manual Chromium check (agent-browser CLI not installed)."
    },
    {
      "id": "US-015",
      "title": "Two Realities scroll-locked PII redaction",
      "description": "As a user, I want to see the left card's PII fields dissolve top-down into redacted blocks as I scroll, morphing into the right ZK-compliant state.",
      "acceptanceCriteria": [
        "frontend/src/components/landing/two-realities-section.tsx implements the pinned split view per Design Spec \u00a74 \u00a73",
        "Left card (Without ZK) shows Recipient/Tax ID/Country/Amount + Danger pill with Iconoir Warning triangle + VIOLATES GDPR\u00b7LGPD\u00b7MiCA label",
        "Right card (With ZK) shows same labels with \u2593\u2593\u2593\u2593\u2593\u2593\u2593 redacted values + Proof 0xa3f8...c91b in mono forest with Iconoir Check + Forest pill CHECK + COMPLIANT\u00b7VERIFIED on mint bg",
        "Caption underneath both: 'Both prove the user is verified. Only one can stand in court.'",
        "On desktop: section pins while PII characters dissolve top-down into blocks as scroll progresses; pin releases when both states are fully visible",
        "On <768px: stacks vertically with sequential fade-in (no pin) via gsap.matchMedia",
        "prefers-reduced-motion: both cards render final state immediately, no redaction animation",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 15,
      "passes": true,
      "notes": "Rewrote src/components/landing/two-realities-section.tsx as a 'use client' component. Narrative: LEFT card (Without ZK) renders plaintext values + danger pill with Iconoir WarningTriangle (VIOLATES GDPR · LGPD · MiCA); the RIGHT card (With ZK) renders redacted ▓ values + mint-bg forest/success pill with Iconoir Check (COMPLIANT · VERIFIED) + the Proof: 0xa3f8...c91b line in mono forest. Caption below both cards. On desktop the wrapper uses PinnedSection (pinDuration 1.5, onProgress → setProgress) so a scrubbed ScrollTrigger drives per-row dissolve of the LEFT card's values: rowProgress(progress, i, 4) staggers windows by (1-0.4)/(n-1) stride so row 0 animates [0, 0.4] → row 3 animates [0.6, 1.0]; dissolve(value, p) replaces the leading Math.ceil(p*len) characters with ▓ block chars so the plain text visually morphs top-down into blocks that mirror the RIGHT card's redacted state by pin release. On <768px PinnedSection short-circuits (isDesktop-only) AND a dedicated brandMatchMedia handler fades each [data-reality-card] in sequentially (opacity 0→1 / y 16→0 / 0.6s / 150 ms stagger, power2.out) via ScrollTrigger (trigger: card, start: 'top 85%'); cleanup resets opacity/transform with gsap.set clearProps so re-mounts don't leave inline styles. prefers-reduced-motion uses useReducedMotion() to force effectiveProgress = 1 — LEFT card renders fully-blocked final state immediately with no rAF/scroll subscription, RIGHT card is static anyway; the brandMatchMedia handler's isReduced branch is intentionally absent (returns undefined = no-op). Accessibility: each dd carries aria-label with the canonical plain/redacted text while the visual span is aria-hidden so SRs don't read the intermediate ▓-mixed states. Pills use badgeVariants({variant:'danger'|'success'}) composed with `inline-flex items-center gap-1.5` so the Iconoir glyph aligns at 12×12. Build ✅ landing first-load 163 kB → 165 kB (+2 kB for iconoir icons + client wrapper; well under US-032 220 kB budget), typecheck ✅, lint ✅, 19 tests ✅. Dev server curl confirms both card titles, both pill labels, caption, proof line all render in server HTML. Manual browser verification of 60fps scrub + dissolve timing requires Chromium (agent-browser CLI still not installed on this machine)."
    },
    {
      "id": "US-016",
      "title": "CountUp primitive + Benchmarks numbers section",
      "description": "As a user, I want the 4 benchmark numbers (<5s, <$0.001, 0, 256 bytes) to count up into view when they enter the viewport.",
      "acceptanceCriteria": [
        "frontend/src/components/motion/count-up.tsx ('use client') accepts target, duration, formatter, teaseTo props; animates from 0 to target on viewport entry",
        "<5s teases e.g. 9.99 \u2192 <5s (not straight count)",
        "0 teases down from a non-zero value to 0",
        "frontend/src/components/landing/numbers-section.tsx renders 4 numbers in 2x2 grid (display-xl) with mono-l sub-labels",
        "prefers-reduced-motion: renders final value without animation",
        "Scrolling the landing triggers CountUp for all four numbers",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 16,
      "passes": true,
      "notes": "CountUp primitive in src/components/motion/count-up.tsx uses motion v12 useInView (once, amount: 0.5) + rAF with ease-out-expo. BenchmarkNumber client wrapper in src/components/landing/benchmark-number.tsx holds the per-label configs (can't pass function props from server component). teaseTo values: 9.99→<5s, 0.01→<$0.001, 99→0, 0→256 bytes. Reduced-motion renders target instantly. SSR shows canonical target (mounted gate) to avoid hydration flash. Landing first-load 163→166 kB."
    },
    {
      "id": "US-017",
      "title": "FadeIn pass on remaining static sections",
      "description": "As a user, I want subtle fade-ins on Paradox, How it works, Use Cases, Developers, Momentum, Closing CTA so the page doesn't snap into view.",
      "acceptanceCriteria": [
        "frontend/src/components/motion/fade-in.tsx ('use client') wraps children and fades in via motion v11 whileInView with brand easing cubic-bezier(0.32,0.72,0,1)",
        "Paradox: headline pins at top; body paragraphs fade in line-by-line",
        "How it works: each of 3 steps fades in sequentially as viewport entries; SVG diagrams reveal via stroke-dasharray",
        "Use Cases, Developers, Momentum, Closing CTA: cards/columns fade + translateY(8px\u21920) on entry",
        "prefers-reduced-motion disables all fades (immediate render)",
        "No stacked ScrollTriggers leak when navigating away and back",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 17,
      "passes": true,
      "notes": "Authored src/components/motion/fade-in.tsx — 'use client' primitive accepting {children, delay, duration=0.6, y=8, amount=0.3, as, className, id}. Uses motion v12 whileInView with viewport {once:true, amount} and transition {ease: BRAND_EASE = cubic-bezier(0.32,0.72,0,1)}. useReducedMotion() short-circuits to a plain JSX element (div/p/li/article/section/span) with no motion import, no animation. The `as` prop enables polymorphic rendering via motion[as] cast to ComponentType<HTMLMotionProps<'div'>> — valid because all motion HTML factories share the same props shape. Authored src/components/landing/step-diagram.tsx — 'use client' SVG diagram component with three per-index schematics (01: user → issuer → Merkle root, 02: device → proof lock+check, 03: proof → on-chain ledger). Each shape (motion.circle / motion.rect / motion.path) animates pathLength 0→1 + opacity 0→1 via whileInView with staggered per-shape delays. useReducedMotion branches to plain <circle>/<rect>/<path> with no animation. Updated src/components/landing/paradox-section.tsx — headline column now md:sticky top-24 (editorial scroll-past pattern), body text split via `/(?<=\\.)\\s+/` regex into sentences rendered as <FadeIn as='p'> with 0.08s stagger each. Updated how-it-works-section.tsx — each step is <FadeIn as='li'> with 0.12s stagger + inline <StepDiagram index delay>. Updated use-cases-section.tsx — UseCaseCard wrapped in <FadeIn as='article'> with (i * 0.08)s stagger per row. Updated developers-section.tsx — code block column and install aside each wrapped in FadeIn with 0/0.12s delays. Updated momentum-section.tsx — 3 columns FadeIn as='article' with 0.1s stagger + footnote FadeIn. Updated closing-cta.tsx — headline, sub, cta row each own FadeIn with 0/0.1/0.2s delays. Verification: typecheck ✅, lint ✅, 19 tests ✅, build ✅ — landing route 95.3 kB / 206 kB first-load (was 166 kB before US-017; +40 kB for motion.svg + motion HTML factories). Under US-032 220 kB budget (14 kB headroom). Dev server curl confirms all 5 paradox sentences split + rendered as separate <p style='opacity:0;transform:translateY(8px)'>, 3 StepDiagram SVGs present (viewBox 0 0 98 24), all use-case names + momentum columns + closing CTA text in SSR. Motion's whileInView pattern + useReducedMotion hook means there are no GSAP ScrollTriggers in any of these sections — no leaks possible on route navigation (unmount cleanup is handled by motion internally)."
    },
    {
      "id": "US-018",
      "title": "Deterministic proof-bytes + demo-script data",
      "description": "As a demo, I need a pre-generated valid Groth16 proof byte sequence and a deterministic script of terminal lines so the simulation is reproducible.",
      "acceptanceCriteria": [
        "frontend/src/lib/proof-bytes.ts exports VALID_PROOF (hex string, 256 bytes), NULLIFIER (hex), EXPIRED_PROOF_ERROR constant",
        "frontend/src/lib/demo-script.ts exports DEMO_STEPS array: [{label, durationMs, status}] totaling 4,710ms wall-clock covering 4 steps (credential, Merkle path, Poseidon, Groth16)",
        "Also exports EXPIRED_DEMO_STEPS where step 3 fails with proof rejected \u00b7 credential expired (block 287,901,433)",
        "DEVNET_TX_HASH constant matches a real pre-existing Solscan devnet transaction (placeholder allowed; documented in comment)",
        "Vitest test asserts VALID_PROOF length and DEMO_STEPS total duration",
        "pnpm test passes",
        "Typecheck passes"
      ],
      "priority": 18,
      "passes": true,
      "notes": "Authored `src/lib/proof-bytes.ts` with VALID_PROOF (0x + 512 hex chars = 256 bytes, matches the Benchmarks \"256 bytes\" claim), NULLIFIER (32 bytes), EXPIRED_BLOCK (287_901_433), EXPIRED_PROOF_ERROR (interpolated en-US formatted block number so the string matches the design spec verbatim), DEVNET_TX_HASH (placeholder signature — commented as swap-before-demo-gate), SOLSCAN_URL (derived). Authored `src/lib/demo-script.ts` with DEMO_STEPS (4 × {label, durationMs, status} summing to exactly 4710 ms: 320 + 540 + 1180 + 2670) and EXPIRED_DEMO_STEPS (3 steps, step 3 status=\"fail\" with error=EXPIRED_PROOF_ERROR). Added optional `error?: string` on DemoStep so US-019 can render the danger-text line without inventing a parallel outcome type. Authored `src/lib/demo-script.test.ts` asserting VALID_PROOF hex length = 512 (= 256 bytes), NULLIFIER hex length = 64, DEMO_STEPS total duration = 4710 exactly, and EXPIRED_DEMO_STEPS halts at step 3 with the canonical error. typecheck + lint + test (27 passed) + build all pass. Landing first-load stayed at 206 kB — these lib modules are tree-shaken until US-019 imports them."
    },
    {
      "id": "US-019",
      "title": "Demo section \u2014 form + proof console",
      "description": "As a user, I want to click Generate proof and see a 4-step terminal log run in real wall-clock 4.71s, then submit the proof to devnet and see a Solscan link.",
      "acceptanceCriteria": [
        "frontend/src/components/landing/demo-section.tsx renders two-column: form left, proof console right",
        "Form contains: Recipient wallet text input (pre-filled 5g8H4nP3eR...), Amount slider 100-10,000 default 1200, Jurisdiction select (US|EU|UK|BR), 'Generate proof' forest button, Toggle 'Try with expired credential'",
        "frontend/src/components/landing/proof-console.tsx renders a Berkeley Mono terminal block on surface-deep",
        "Click Generate proof: steps [1/4]..[4/4] appear sequentially using real setTimeouts from DEMO_STEPS; terminal concludes with proof: 0x..., nullifier: 0x..., duration: 4.71s",
        "Button morphs to 'Submit to devnet \u2192'; click pauses 1.5s then appends 'Transaction confirmed \u00b7 {hash} \u00b7 View on Solscan \u2197' linking to the devnet tx",
        "Expired flow: [3/4] fails in danger-text 'proof rejected \u00b7 credential expired (block 287,901,433)'",
        "Honesty footer below demo: 'Simulation. Click View on Solscan to verify the hash is real on-chain.' in mono muted small",
        "aria-live polite region announces state changes",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 19,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Shiki SSR code block for Developers section",
      "description": "As a developer, I want the SDK 3-line TypeScript example rendered with syntax highlighting using Shiki SSR so no client JS is added.",
      "acceptanceCriteria": [
        "frontend/src/lib/shiki.ts defines a custom theme using design tokens (forest, ink, stone, muted) and exports a codeToHtml helper",
        "frontend/src/components/ui/code-block.tsx (Server Component) renders highlighted HTML via Shiki, accepts code/lang props",
        "frontend/src/components/landing/developers-section.tsx uses CodeBlock to render the 3-line SDK example (import { zksettle } ... await connection.sendTransaction(tx);)",
        "Tabs component (client) toggles TypeScript | Rust | Anchor CPI; non-TS tabs show 'Coming soon \u00b7 TypeScript first'",
        "Side panel has install command, copy button, version v0.1.0, GitHub link, 'MIT licensed \u00b7 Open source from day one'",
        "No Shiki JS runs on the client (verified via build output)",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-021",
      "title": "Mock data layer for dashboard",
      "description": "As a dashboard, I need typed mock data for issuers, transactions, audit events, policies, api keys, team, billing so all pages render without a backend.",
      "acceptanceCriteria": [
        "frontend/src/lib/mock-data.ts exports: ISSUERS (6 entries: Persona, Sumsub, Onfido, Jumio, Veriff, MockKYC), INITIAL_TRANSACTIONS (100 rows), AUDIT_EVENTS (>200 rows spanning 30 days), BILLING_USAGE (30 days), INVOICES (3), TEAM, POLICIES, API_KEYS",
        "All arrays are typed via exported interfaces (no any)",
        "Transaction shape: {id, time, wallet, issuerId, status: verified|blocked|pending, amount, currency, jurisdiction, txHash, proofHash, slot, cu}",
        "generateLiveEvent(seed, index) returns a deterministic next transaction for the live feed",
        "Vitest test confirms generateLiveEvent is deterministic for a fixed seed",
        "pnpm test passes",
        "Typecheck passes"
      ],
      "priority": 21,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-022",
      "title": "Dashboard chrome \u2014 sidebar, topbar, layout",
      "description": "As a user, I want the dashboard's sidebar and topbar to frame every /dashboard/* route per the design system's 3-group IA.",
      "acceptanceCriteria": [
        "frontend/src/app/dashboard/layout.tsx composes Sidebar + TopBar + main content area; max-width 1280px, padding 32px",
        "frontend/src/components/dashboard/sidebar.tsx: 240px fixed width, surface bg, border-right; mini wordmark top; workspace switcher (Acme Stablecoin \u25be); 3 eyebrow-labeled groups (OVERVIEW, CONTROLS, ACCOUNT) with Iconoir icons; active state = surface-deep row bg + 2px forest vertical rule on left edge + ink text",
        "frontend/src/components/dashboard/top-bar.tsx: 56px, canvas bg, hairline border-bottom only on scroll; page title display-m left with 1-line stone subtitle; right side \u2318K hint, bell icon with forest dot, avatar circle",
        "frontend/src/app/dashboard/page.tsx redirects to /dashboard/transactions",
        "Clicking each of 8 sidebar links navigates to the respective route without console errors (even if page is placeholder)",
        "Keyboard navigation: Tab moves through sidebar items, active link has 2px forest focus outline",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 22,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-023",
      "title": "StatusPill + StatCard + TransactionRow primitives",
      "description": "As a developer, I need reusable dashboard primitives so tables and headers share one visual language.",
      "acceptanceCriteria": [
        "frontend/src/components/dashboard/status-pill.tsx renders Iconoir glyph + label in one of: verified (mint bg, forest text, Check glyph), blocked (danger-bg, danger-text, Xmark), warning (warning-bg, warning-text, Warning triangle), info (info-bg, info-text, InfoCircle), test (surface-deep, muted, Sparks)",
        "StatusPill never renders a bare colored dot \u2014 always Iconoir + label",
        "frontend/src/components/dashboard/stat-card.tsx renders display-m number + mono-l sub-label on surface with border-subtle",
        "frontend/src/components/dashboard/transaction-row.tsx renders a table row with 2px left vertical rule (emerald|danger-text|muted by status), columns Time/Wallet/Issuer/Status pill/Amount/Jurisdiction/Tx, hover reveals View proof bytes \u25be",
        "Stories/demo route shows all primitives",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 23,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-024",
      "title": "/dashboard/transactions \u2014 live feed (Tier A)",
      "description": "As a user, I want the default dashboard to be a live feed of settlement events with stat cards, filters, and a pseudo-realtime ticker.",
      "acceptanceCriteria": [
        "frontend/src/app/dashboard/transactions/page.tsx renders page subtitle 'Live feed of settlement events. Every row carries a proof; every proof is replayable six months from now.'",
        "4 stat cards at top: 1,847 proofs verified (+12% vs yesterday), 23 blocked (1.2% rejection rate), 4.7s avg proving (p95 6.2s), $0.00091 avg verify cost (Devnet)",
        "Filter bar: toggles All | Verified | Blocked + date range picker (visual only)",
        "Table uses TransactionRow; columns align with component",
        "Pseudo-realtime: setInterval pushes a new event every 3-8s (deterministic by seed, URL ?seed=N override), new row fades in, DOM capped at 100 rows",
        "Cleanup setInterval on route change (no zombie timers)",
        "Zero console errors",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 24,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-025",
      "title": "/dashboard/counterparties \u2014 issuer list (Tier A)",
      "description": "As a user, I want a table of registered issuers showing their Merkle roots, users, last update, and status.",
      "acceptanceCriteria": [
        "frontend/src/app/dashboard/counterparties/page.tsx renders page subtitle 'Issuers that have published a Merkle root your policies trust.'",
        "Table columns: Name, Pubkey, Merkle root, Users, Last update, Status pill",
        "6 rows from ISSUERS (Persona, Sumsub, Onfido, Jumio, Veriff, MockKYC) with status pills per design: Check+Active forest, Warning triangle+Stale, Sparks+Test",
        "CTA top right: 'Register issuer \u2192' forest button opens a modal scaffold with 'Available in private beta' footer (click closes modal)",
        "All addresses rendered in Berkeley Mono truncated via format.ts",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-026",
      "title": "/dashboard/audit-log \u2014 exportable history (Tier A)",
      "description": "As a user, I want a paginated audit log of every attestation with filters and export buttons (mock).",
      "acceptanceCriteria": [
        "frontend/src/app/dashboard/audit-log/page.tsx renders page subtitle 'Every attestation, exportable on request.'",
        "Filter bar: date range, issuer dropdown, status, jurisdiction",
        "Table columns: Time, Wallet, Issuer, Status pill, Amount, Jurisdiction, Proof hash, Block, Slot, CU consumed, Tx",
        "Export bar top right: Export CSV, Export JSON, Webhook digest (ghost buttons). Click toasts 'Available in private beta \u00b7 request access \u2197'",
        "Pagination uses Berkeley Mono '\u2190 1 2 3 ... 47 \u2192'",
        "Footer line: 'Showing 50 of 23,481 attestations \u00b7 Last 30 days'",
        "Zero console errors",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 26,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-027",
      "title": "/dashboard/billing \u2014 usage + invoices (Tier A)",
      "description": "As a user, I want to see my current tier, 30-day usage chart, and last 3 invoices.",
      "acceptanceCriteria": [
        "frontend/src/app/dashboard/billing/page.tsx renders page subtitle 'Pay for what you prove.'",
        "Three stacked cards: Current tier (Startup \u00b7 50,000 proofs/mo \u00b7 $0.05/proof), Used this month 18,432 (37%) with forest progress bar; Usage chart (30-day line chart, mono axis labels in muted, line in forest, no fill, hairline grid); Invoices table (3 rows, Download PDF ghost buttons toasting 'Available in private beta')",
        "Chart built with SVG or a lightweight lib; no client-heavy chart lib",
        "Progress bar and chart colors use forest and border-subtle tokens only",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 27,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-028",
      "title": "TierBScaffold + 4 scaffolded routes",
      "description": "As a user, I want attestations, policies, api-keys, and team routes to render the same chrome with a clean coming-soon empty state.",
      "acceptanceCriteria": [
        "frontend/src/components/dashboard/tier-b-scaffold.tsx accepts {icon, title, body} and renders a centered empty state: 64px Iconoir glyph in ghost + display-m headline + one-line stone body + 'Request access \u2197' ghost button",
        "frontend/src/app/dashboard/attestations/page.tsx uses TierBScaffold with Check glyph, 'Attestation explorer \u00b7 coming soon', 'Filter, search, and inspect every ComplianceAttestation. Available to private-beta participants.'",
        "frontend/src/app/dashboard/policies/page.tsx uses TierBScaffold with Page glyph and policies copy",
        "frontend/src/app/dashboard/api-keys/page.tsx uses TierBScaffold with Key glyph and api keys copy",
        "frontend/src/app/dashboard/team/page.tsx uses TierBScaffold with Group glyph and team copy",
        "All 4 routes share the Dashboard layout (sidebar + topbar + page title + subtitle)",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 28,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-029",
      "title": "Mobile fallbacks audit + fixes",
      "description": "As a mobile user, I want the landing to remain readable and usable without jank.",
      "acceptanceCriteria": [
        "Viewport <768px: Veil canvas swaps to a static SVG placeholder",
        "Two Realities pin disabled; cards stack vertically with sequential fade-in",
        "Dashboard sidebar collapses to a hamburger drawer (tap to open/close, focus trap active when open)",
        "Section vertical rhythm reduced to 96px per spec",
        "Outer page gutter 20px per spec",
        "All CTAs remain full-width tappable (min 44x44px hit area)",
        "Lighthouse mobile: Performance \u2265 85, A11y \u2265 95, SEO \u2265 95",
        "Typecheck passes",
        "Verify in browser using dev-browser skill (emulate iPhone 15 viewport)"
      ],
      "priority": 29,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-030",
      "title": "Accessibility pass",
      "description": "As a keyboard or assistive-tech user, I need every interactive surface reachable, announced, and reduced-motion aware.",
      "acceptanceCriteria": [
        "Skip-to-content link focusable on Tab at the top of every page",
        "All buttons/inputs/links have 2px forest focus outline with 2px offset",
        "Demo section aria-live='polite' announces proof-generation state changes",
        "Dashboard sidebar has aria-current='page' on active route; all nav links have visible label (not icon only)",
        "prefers-reduced-motion disables GSAP pins, Three.js animation loop, and motion fades (verified via CDP override)",
        "Contrast: ink on canvas \u2265 12:1, forest on canvas \u2265 10:1 (verified via axe or manual)",
        "axe-core or Lighthouse a11y \u2265 95 on landing AND /dashboard/transactions",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 30,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-031",
      "title": "OG image route + favicon + SEO metadata",
      "description": "As a link share, I want a proper OG preview and favicon so the site looks intentional everywhere.",
      "acceptanceCriteria": [
        "frontend/src/app/og/route.tsx exports GET returning an ImageResponse (1200\u00d7630, forest bg, ZKSettle Georgia 96px + subline 'Compliance-grade rails for stablecoin settlement.' 36px)",
        "frontend/public/favicon.svg exists as the Seal-only variant",
        "metadata.openGraph.images in layout.tsx points to /og",
        "metadata.icons.icon points to /favicon.svg",
        "metadata.twitter.card = 'summary_large_image', title, description, images /og",
        "Visiting /og renders a valid 1200\u00d7630 image in the browser",
        "Typecheck passes",
        "Verify in browser using dev-browser skill (navigate to /og)"
      ],
      "priority": 31,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-032",
      "title": "Lighthouse pass + performance fixes",
      "description": "As a user, I want the landing and dashboard to meet the spec's performance budget.",
      "acceptanceCriteria": [
        "pnpm build produces a first-load JS on landing \u2264 220KB gzipped (verified via build output)",
        "pnpm build produces a first-load JS on /dashboard/transactions \u2264 120KB gzipped",
        "next.config.ts has experimental.optimizePackageImports including 'iconoir-react'",
        "Three.js imports are tree-shaken (named imports, not import * as THREE)",
        "Lighthouse run on http://localhost:3000 (after pnpm build && pnpm start): Performance \u2265 85, A11y \u2265 95, SEO \u2265 95, LCP < 2.5s, INP < 200ms",
        "Lighthouse run on http://localhost:3000/dashboard/transactions: Performance \u2265 85, A11y \u2265 95, SEO \u2265 95",
        "Lighthouse HTML reports saved to frontend/lighthouse-landing.html and frontend/lighthouse-dashboard.html",
        "Typecheck passes"
      ],
      "priority": 32,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-033",
      "title": "Smoke test every flow end-to-end",
      "description": "As a QA, I want a documented manual smoke that proves every page, CTA, and animation works at least in Chromium.",
      "acceptanceCriteria": [
        "Landing loads at / with no console errors; hero canvas renders or static fallback activates",
        "Scroll through all 10 sections \u2014 fades and Two Realities pin work (no stuck pins)",
        "Demo flow: click Generate proof, see 4-step animation, submit-to-devnet morph, Solscan link opens in new tab",
        "Try expired credential toggle \u2192 [3/4] fails in danger-text",
        "/dashboard redirects to /dashboard/transactions",
        "Sidebar navigation to each of 8 routes works without console errors",
        "Cmd-K visual hint present (non-functional)",
        "Typecheck passes",
        "Verify in browser using dev-browser skill (run the full flow)"
      ],
      "priority": 33,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-034",
      "title": "Finalize README for submission",
      "description": "As a Colosseum judge, I want a clear README covering stack, run, build, test, deploy, and design system links.",
      "acceptanceCriteria": [
        "frontend/README.md contains: project summary, Stack section (Next.js 15, React 19, TS strict, Tailwind v4, Three.js, GSAP, motion, Shiki, Iconoir, Geist + Berkeley Mono fallback)",
        "Run section: pnpm install + pnpm dev",
        "Build section: pnpm build + pnpm start",
        "Test section: pnpm test, pnpm typecheck, pnpm lint",
        "Deploy section: Vercel auto-deploy from main plus fallback URL note",
        "Design system section: links to ../docs/superpowers/specs/2026-04-18-zksettle-landing-design.md and the canonical design board URL",
        "No broken relative links",
        "Typecheck passes"
      ],
      "priority": 34,
      "passes": false,
      "notes": ""
    }
  ]
}
